whenever a password changes on the platform, the API Key keys Change and update

Ability to assign Workspaces to the clients

For Enrichment Presets we should have a tag that will show the enrichment types like Apollo, Builtwith.

We should also enable what we want to allow for enrichments and what we do not want to allow which will then connect to workflows as an option for enrichment block.

Hubspot Integration layer with: 	
•	    Identity resolution
	•	domain normalization
	•	name normalization
	•	dedupe matching
	•	canonical account/contact match


Do we add Webhooks and Triggers to this system

Recreate the Clay webhook System and API send out system which should be in workflows 
- Assign to Campaign
- Get Email
- Get Phone Numbers

Is there a way to create this for self-hosted companies with SOC2 Compliance, providing a license model?

Designed for mid-market enterprise where they use slack as there primary operation but the backend system is configurable to meet the needs of a BDR?

$1000 a month for 15 users and $99 per additional user

Also want to confirm that this application is a passthrough and does not hold any data longer than 30 - 90 days. So essentially the data must be stored somewhere else. OTHER Than the LOGS that relate to the application, NOT that relate to the Clients,

