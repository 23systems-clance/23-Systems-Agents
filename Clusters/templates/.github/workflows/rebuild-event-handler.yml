name: Rebuild Event Handler

on:
  push:
    branches: [main]

concurrency:
  group: event-handler-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: self-hosted
    timeout-minutes: 20
    steps:
      - name: Pull latest and detect version change
        id: pull
        run: |
          docker exec 23wf-event-handler bash -c '
            export GH_TOKEN=$(grep "^GH_TOKEN=" /app/.env | cut -d= -f2-)
            echo "${GH_TOKEN}" | gh auth login --with-token
            gh auth setup-git

            git fetch origin main
            CHANGED=$(git diff --name-only HEAD origin/main)
            if ! echo "$CHANGED" | grep -qv "^logs/"; then
              echo "SKIP" > /app/.rebuild-status
              git reset --hard origin/main
              exit 0
            fi

            # Detect 23wf version change from package-lock.json in git
            git show HEAD:package-lock.json > /tmp/old-lock.json 2>/dev/null || echo "{}" > /tmp/old-lock.json
            git show origin/main:package-lock.json > /tmp/new-lock.json
            OLD_TPB=$(node -p "try{require(\"/tmp/old-lock.json\").packages[\"node_modules/23wf\"]?.version||\"\"}catch(e){\"\"}")
            NEW_TPB=$(node -p "try{require(\"/tmp/new-lock.json\").packages[\"node_modules/23wf\"]?.version||\"\"}catch(e){\"\"}")
            rm -f /tmp/old-lock.json /tmp/new-lock.json

            git reset --hard origin/main

            if [ -n "$OLD_TPB" ] && [ "$OLD_TPB" != "$NEW_TPB" ]; then
              # Version changed — run 23wf init to scaffold new templates
              if echo "$NEW_TPB" | grep -q "-"; then
                npx --yes "23wf@${NEW_TPB}" init
              else
                npx --yes 23wf@latest init
              fi

              # Commit any template changes from init
              git add -A
              if ! git diff --cached --quiet; then
                git commit -m "chore: apply 23wf init after upgrade"
                git push origin main
              fi

              # Update WF_VERSION in .env so docker compose pulls the right image
              if grep -q "^WF_VERSION=" /app/.env; then
                sed -i "s/^WF_VERSION=.*/WF_VERSION=$NEW_TPB/" /app/.env
              else
                echo "WF_VERSION=$NEW_TPB" >> /app/.env
              fi
              echo "VERSION_CHANGED" > /app/.rebuild-status
            else
              npm install --omit=dev
              echo "REBUILD" > /app/.rebuild-status
            fi
          '

          STATUS=$(docker exec 23wf-event-handler cat /app/.rebuild-status)
          docker exec 23wf-event-handler rm -f /app/.rebuild-status
          echo "status=$STATUS" >> $GITHUB_OUTPUT

      - name: Rebuild (no version change)
        if: steps.pull.outputs.status == 'REBUILD'
        run: |
          docker exec 23wf-event-handler bash -c '
            rm -rf .next-new .next-old
            NEXT_BUILD_DIR=.next-new npm run build

            mv .next .next-old 2>/dev/null || true
            mv .next-new .next

            echo "Rebuild complete, reloading Next.js..."
            npx pm2 reload all

            rm -rf .next-old
          '

      - name: Pull new image and restart container
        if: steps.pull.outputs.status == 'VERSION_CHANGED'
        run: |
          HOST_DIR=$(docker inspect 23wf-event-handler --format '{{index .Config.Labels "com.docker.compose.project.working_dir"}}')
          docker compose -f /project/docker-compose.yml --project-directory "$HOST_DIR" --env-file /project/.env pull event-handler
          docker stop 23wf-event-handler || true
          docker rm 23wf-event-handler || true
          docker compose -f /project/docker-compose.yml --project-directory "$HOST_DIR" --env-file /project/.env up -d event-handler

      - name: Rebuild in new container
        if: steps.pull.outputs.status == 'VERSION_CHANGED'
        run: |
          echo "Waiting for new container..."
          for i in $(seq 1 30); do
            if docker exec 23wf-event-handler echo "ready" 2>/dev/null; then
              break
            fi
            sleep 2
          done

          docker exec 23wf-event-handler bash -c '
            npm install --omit=dev

            rm -rf .next-new .next-old
            NEXT_BUILD_DIR=.next-new npm run build

            mv .next .next-old 2>/dev/null || true
            mv .next-new .next

            npx pm2 restart all

            rm -rf .next-old
          '
