name: Upgrade Event Handler

on:
  workflow_dispatch:
    inputs:
      target_version:
        description: 'Target version to install (empty for latest stable)'
        required: false
        default: ''

concurrency:
  group: event-handler-deploy
  cancel-in-progress: false

jobs:
  upgrade:
    runs-on: self-hosted
    timeout-minutes: 20
    steps:
      - name: Upgrade 23wf
        run: |
          docker exec 23wf-event-handler bash -c '
            export GH_TOKEN=$(grep "^GH_TOKEN=" /app/.env | cut -d= -f2-)
            echo "${GH_TOKEN}" | gh auth login --with-token
            gh auth setup-git

            REPO_URL=$(git -C /app remote get-url origin)
            WORK_DIR=$(mktemp -d)
            git clone --depth 1 "$REPO_URL" "$WORK_DIR"
            cd "$WORK_DIR"

            git config user.name "23wf"
            git config user.email "23wf@users.noreply.github.com"

            npm install --omit=dev
            OLD_VERSION=$(node -p "require(\"./node_modules/23wf/package.json\").version")

            TARGET="${{ github.event.inputs.target_version }}"
            if [ -n "$TARGET" ] && echo "$TARGET" | grep -q "-"; then
              # Beta: npm update cannot upgrade an exact-pinned dependency, must use install
              npm install "23wf@${TARGET}" --prefer-online
            else
              # Stable: npm update resolves to latest within semver range
              npm update 23wf --prefer-online
            fi

            NEW_VERSION=$(node -p "require(\"./node_modules/23wf/package.json\").version")

            if [ "$OLD_VERSION" != "$NEW_VERSION" ]; then
              BRANCH="upgrade/23wf-${NEW_VERSION}-$(date +%s)"
              git add -A
              git checkout -b "$BRANCH"
              git commit -m "chore: upgrade 23wf to $NEW_VERSION"
              git push origin "$BRANCH"
              gh pr create --title "chore: upgrade 23wf" --body "Automated upgrade via upgrade-event-handler workflow." --base main --head "$BRANCH"
              gh pr merge "$BRANCH" --squash --auto --delete-branch
            else
              echo "No changes — 23wf is already up to date."
            fi

            rm -rf "$WORK_DIR"
          '
